// Compatibility page for older controllers/routes that still render
// Inertia::render('dashboard/index'). The canonical page remains dashboard.tsx.
import DashboardPage from '../dashboard';

export default DashboardPage;
